<UserControl x:Class="TriggerPoint.Views.StochasticDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid AutoGeneratingColumn="DataGrid_AutoGeneratingColumn" EnableRowVirtualization="True" CanUserAddRows="False" CanUserDeleteRows="False" 
                  AutoGenerateColumns="True" ItemsSource="{Binding ScriptData, IsAsync=True}"/>
        <ProgressBar Height="5" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Minimum="0" Maximum="1" Value="{Binding Progress}" Visibility="{Binding IsLoading,Converter={StaticResource BooleanToVisibilityConverter}}"/>

    </Grid>
</UserControl>
